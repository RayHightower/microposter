<%= form_for(@jackrabbit) do |f| %>
  <% if @jackrabbit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jackrabbit.errors.count, "error") %> prohibited this jackrabbit from being saved:</h2>

      <ul>
      <% @jackrabbit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
